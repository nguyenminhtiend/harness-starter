{
  "name": "harness-starter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "api": "bun run --filter @harness/example-api dev",
    "console": "bun run --filter @harness/example-console dev",
    "web": "bun run api & bun run console",
    "studio:dev": "bun run --filter @harness/studio dev",
    "studio:build": "bun run --filter @harness/studio build",
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "ci": "bun run lint && bun run typecheck && bun run build && bun test",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.12",
    "@changesets/cli": "2.30.0",
    "@commitlint/cli": "20.5.0",
    "@commitlint/config-conventional": "20.5.0",
    "@commitlint/types": "20.5.0",
    "lefthook": "2.1.6",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "gpt-tokenizer": "3.4.0",
    "ollama-ai-provider-v2": "3.5.0"
  }
}
