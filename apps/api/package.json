{
  "name": "@harness/example-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@harness/bootstrap": "workspace:*",
    "@harness/http": "workspace:*",
    "@harness/mastra": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
