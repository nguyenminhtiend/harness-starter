{
  "$schema": "https://biomejs.dev/schemas/2.4.12/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "ignoreUnknown": true,
    "includes": [
      "**",
      "!**/dist",
      "!**/node_modules",
      "!**/*.d.ts",
      "!**/bun.lock",
      "!**/.changeset",
      "!**/.harness",
      "!**/sample-ui"
    ]
  },
  "assist": {
    "enabled": true,
    "actions": { "source": { "organizeImports": "on" } }
  },
  "formatter": {
    "enabled": true,
    "formatWithErrors": false,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineEnding": "lf",
    "lineWidth": 100
  },
  "javascript": {
    "formatter": {
      "arrowParentheses": "always",
      "jsxQuoteStyle": "double",
      "quoteProperties": "asNeeded",
      "quoteStyle": "single",
      "semicolons": "always",
      "trailingCommas": "all"
    }
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "complexity": {
        "noForEach": "off"
      },
      "correctness": {
        "noUnusedImports": "error",
        "noUnusedVariables": "error",
        "noUnusedFunctionParameters": "warn"
      },
      "style": {
        "noNonNullAssertion": "warn",
        "useBlockStatements": "error",
        "useImportType": "error",
        "useNodejsImportProtocol": "error"
      },
      "suspicious": {
        "noConsole": "error",
        "noExplicitAny": "error"
      },
      "performance": {
        "noAccumulatingSpread": "warn",
        "noDelete": "warn"
      }
    }
  },
  "overrides": [
    {
      "includes": [
        "**/__tests__/**",
        "**/test/**",
        "**/*.test.ts",
        "**/*.test.tsx",
        "**/*.spec.ts",
        "**/e2e/**"
      ],
      "linter": {
        "rules": {
          "suspicious": {
            "noConsole": "off"
          }
        }
      }
    },
    {
      "includes": ["packages/core/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@harness/agent": "core must not import from @harness/agent (dependency direction violation)",
                  "@harness/tools": "core must not import from @harness/tools (dependency direction violation)",
                  "@harness/memory-sqlite": "core must not import from @harness/memory-sqlite (dependency direction violation)",
                  "@harness/mcp": "core must not import from @harness/mcp (dependency direction violation)",
                  "@harness/observability": "core must not import from @harness/observability (dependency direction violation)",
                  "@harness/eval": "core must not import from @harness/eval (dependency direction violation)",
                  "@harness/cli": "core must not import from @harness/cli (dependency direction violation)",
                  "@harness/llm-adapter": "core must not import from @harness/llm-adapter (dependency direction violation)",
                  "@harness/session-store": "core must not import from @harness/session-store (dependency direction violation)",
                  "@harness/session-events": "core must not import from @harness/session-events (dependency direction violation)",
                  "@harness/hitl": "core must not import from @harness/hitl (dependency direction violation)"
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/tools/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@harness/memory-sqlite": "tools must not import from @harness/memory-sqlite (dependency direction violation)",
                  "@harness/mcp": "tools must not import from @harness/mcp (dependency direction violation)",
                  "@harness/observability": "tools must not import from @harness/observability (dependency direction violation)",
                  "@harness/eval": "tools must not import from @harness/eval (dependency direction violation)",
                  "@harness/cli": "tools must not import from @harness/cli (dependency direction violation)"
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/observability/**"],
      "linter": {
        "rules": {
          "suspicious": {
            "noConsole": "off"
          },
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@harness/agent": "observability must not import from @harness/agent (dependency direction violation)",
                  "@harness/tools": "observability must not import from @harness/tools (dependency direction violation)",
                  "@harness/memory-sqlite": "observability must not import from @harness/memory-sqlite (dependency direction violation)",
                  "@harness/mcp": "observability must not import from @harness/mcp (dependency direction violation)",
                  "@harness/eval": "observability must not import from @harness/eval (dependency direction violation)",
                  "@harness/cli": "observability must not import from @harness/cli (dependency direction violation)"
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/memory-sqlite/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@harness/tools": "memory-sqlite must not import from @harness/tools (dependency direction violation)",
                  "@harness/mcp": "memory-sqlite must not import from @harness/mcp (dependency direction violation)",
                  "@harness/observability": "memory-sqlite must not import from @harness/observability (dependency direction violation)",
                  "@harness/eval": "memory-sqlite must not import from @harness/eval (dependency direction violation)",
                  "@harness/cli": "memory-sqlite must not import from @harness/cli (dependency direction violation)"
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/mcp/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@harness/tools": "mcp must not import from @harness/tools (dependency direction violation)",
                  "@harness/memory-sqlite": "mcp must not import from @harness/memory-sqlite (dependency direction violation)",
                  "@harness/observability": "mcp must not import from @harness/observability (dependency direction violation)",
                  "@harness/eval": "mcp must not import from @harness/eval (dependency direction violation)",
                  "@harness/cli": "mcp must not import from @harness/cli (dependency direction violation)"
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/eval/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@harness/cli": "eval must not import from @harness/cli (dependency direction violation)",
                  "@harness/tools": "eval must not import from @harness/tools (dependency direction violation)",
                  "@harness/memory-sqlite": "eval must not import from @harness/memory-sqlite (dependency direction violation)",
                  "@harness/mcp": "eval must not import from @harness/mcp (dependency direction violation)",
                  "@harness/observability": "eval must not import from @harness/observability (dependency direction violation)"
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/llm-adapter/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@harness/agent": "llm-adapter must not import from @harness/agent (dependency direction violation)",
                  "@harness/session-store": "llm-adapter must not import from @harness/session-store (dependency direction violation)",
                  "@harness/session-events": "llm-adapter must not import from @harness/session-events (dependency direction violation)",
                  "@harness/hitl": "llm-adapter must not import from @harness/hitl (dependency direction violation)"
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/session-store/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@harness/core": "session-store must not import from @harness/core (zero harness deps)",
                  "@harness/agent": "session-store must not import from @harness/agent (zero harness deps)",
                  "@harness/llm-adapter": "session-store must not import from @harness/llm-adapter (zero harness deps)",
                  "@harness/session-events": "session-store must not import from @harness/session-events (zero harness deps)",
                  "@harness/hitl": "session-store must not import from @harness/hitl (zero harness deps)"
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/session-events/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@harness/llm-adapter": "session-events must not import from @harness/llm-adapter (dependency direction violation)",
                  "@harness/session-store": "session-events must not import from @harness/session-store (dependency direction violation)",
                  "@harness/hitl": "session-events must not import from @harness/hitl (dependency direction violation)"
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["packages/hitl/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@harness/llm-adapter": "hitl must not import from @harness/llm-adapter (dependency direction violation)",
                  "@harness/session-store": "hitl must not import from @harness/session-store (dependency direction violation)",
                  "@harness/session-events": "hitl must not import from @harness/session-events (dependency direction violation)"
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["apps/**"],
      "linter": {
        "rules": {
          "suspicious": {
            "noConsole": "off"
          }
        }
      }
    },
    {
      "includes": ["packages/cli/**"],
      "linter": {
        "rules": {
          "suspicious": {
            "noConsole": "off"
          },
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@harness/agent": "cli must not import from @harness/agent (dependency direction violation)",
                  "@harness/tools": "cli must not import from @harness/tools (dependency direction violation)",
                  "@harness/memory-sqlite": "cli must not import from @harness/memory-sqlite (dependency direction violation)",
                  "@harness/mcp": "cli must not import from @harness/mcp (dependency direction violation)",
                  "@harness/observability": "cli must not import from @harness/observability (dependency direction violation)"
                }
              }
            }
          }
        }
      }
    }
  ]
}
